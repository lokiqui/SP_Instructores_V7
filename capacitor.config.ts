import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'instructor.skypractice',
  appName: 'skypractice',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
